# 限制与安全边界

- 当前仅分析 Python 文件；BM25 与 Embedding 通道已经可用，但不包含依赖图扩展或跨语言解析。
- 内置自动建议只覆盖分页偏移、可选字段空值与路径穿越三个可复现的受控案例。其他 Issue 只做检索，不臆造修复。
- 补丁是单次文本替换；它不处理复杂重构、合并冲突或二进制文件。
- pytest 仅允许在目标或隔离仓库中运行相对路径；不会执行任意 shell 命令。
- API 不保存密钥，默认没有模型提供方；上线前需要添加认证、速率限制、任务清理及更严格的命令白名单。
- RepoPilot 不会自行提交、推送或创建 Pull Request。
